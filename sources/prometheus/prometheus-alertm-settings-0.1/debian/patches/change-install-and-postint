Description: <short summary of the patch>
 TODO: Put a short summary on the line above and replace this paragraph
 with a longer explanation of this change. Complete the meta-information
 with other relevant fields (see below for details). To make it easier, the
 information below has been extracted from the changelog. Adjust it or drop
 it.
 .
 prometheus-alertm-settings (0.1-3) UNRELEASED; urgency=medium
 .
   [ Nikita ]
   * Some changes
   * Some changes
   * And... some changes
 .
   [ satnik ]
   * Remove secrets from alertmanager.yaml
   * Create template for put secret in config from variable
   * Add in postinst script for check empty variable and used envsubst for
     template
Author: satnik <satnik@mrRobot>

---
The information above should follow the Patch Tagging Guidelines, please
checkout https://dep.debian.net/deps/dep3/ to learn about the format. Here
are templates for supplementary fields that you might want to add:

Origin: (upstream|backport|vendor|other), (<patch-url>|commit:<commit-id>)
Bug: <upstream-bugtracker-url>
Bug-Debian: https://bugs.debian.org/<bugnumber>
Bug-Ubuntu: https://launchpad.net/bugs/<bugnumber>
Forwarded: (no|not-needed|<patch-forwarded-url>)
Applied-Upstream: <version>, (<commit-url>|commit:<commid-id>)
Reviewed-By: <name and email of someone who approved/reviewed the patch>
Last-Update: 2026-09-01

--- /dev/null
+++ prometheus-alertm-settings-0.1/tmp/alertmanager_conf.yml.template
@@ -0,0 +1,32 @@
+route:
+ group_by: ['alertname']
+ group_wait: 30s
+ group_interval: 5m
+ repeat_interval: 1h
+ receiver: 'email-me'
+
+receivers: 
+- name: 'web.hook'
+  webhook_configs:
+  - url: 'http://127.0.0.1:5001/'
+- name: 'email-me'
+  email_configs:
+  - to: '${EMAIL}'
+    from: '${EMAIL}'
+    smarthost: '${SMTP}'
+    auth_username: '${AUTH_UNAME}'
+    auth_identity: '${AUTH_UNAME}'
+    auth_password: '${PASS_AUTH}'
+    
+# Inhibition rules allow to mute a set of alerts given that another alert is
+#firing.
+# We use this to mute any warning-level notifications if the same alert is
+# already critical.
+inhibit_rules:
+ - source_match:
+     severity: 'critical'
+   target_match:
+     severity: 'warning'
+   equal: ['alertname', 'dev', 'instance']
+
+
